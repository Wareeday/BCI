# ── Core scientific stack ─────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ── EEG / signal processing ───────────────────────────────────────
mne>=1.5.0                  # ICA, ASR, epoch extraction, feature extraction
pyserial>=3.5               # OpenBCI serial communication
pylsl>=1.16.0               # LabStreamingLayer

# ── Machine learning ──────────────────────────────────────────────
tensorflow>=2.14.0          # CNN classifier (P300 + motor imagery)
torch>=2.1.0                # EEGNet backup model
scikit-learn>=1.3.0         # LDA, SVM, RandomForest baseline
joblib>=1.3.0               # model serialisation

# ── Streaming / messaging ─────────────────────────────────────────
kafka-python>=2.0.2         # Apache Kafka producer/consumer
confluent-kafka>=2.3.0      # Confluent schema registry client

# ── API / web framework ───────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
pydantic>=2.4.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0   # JWT tokens
passlib[bcrypt]>=1.7.4

# ── Database ──────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9      # PostgreSQL adapter
aiosqlite>=0.19.0           # SQLite for dev/test

# ── Dashboard / visualisation ─────────────────────────────────────
dash>=2.14.0
plotly>=5.18.0
dash-bootstrap-components>=1.5.0

# ── Security / privacy ────────────────────────────────────────────
cryptography>=41.0.0        # AES-256, TLS cert generation
python-dotenv>=1.0.0
boto3>=1.28.0               # AWS KMS (optional, for prod key management)

# ── Privacy-preserving ML ─────────────────────────────────────────
diffprivlib>=0.6.4          # IBM differential privacy library

# ── ROS / device integration ──────────────────────────────────────
# roslibpy>=1.3.0           # Uncomment when running on ROS machine
pyserial>=3.5               # Arduino serial communication

# ── Text-to-speech ────────────────────────────────────────────────
pyttsx3>=2.90               # Offline TTS (Festival backend on Linux)

# ── Testing ───────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.25.0               # async HTTP client for API tests
faker>=19.0.0

# ── Dev tooling ───────────────────────────────────────────────────
black>=23.0.0
flake8>=6.1.0
mypy>=1.6.0
pre-commit>=3.5.0
loguru>=0.7.2               # structured logging